# Email Generator

Minimal Vite + React app with a local email generator component (no external API).

## Quick start

1. Install dependencies
```bash
npm install
npm run dev
npm run build

---

### 8) `src/index.css`
```css
@tailwind base;
@tailwind components;
@tailwind utilities;

body { background: #f8fafc; }
